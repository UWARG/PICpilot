#include <stdio.h>
#include "cameraManager.h"
#include "main.h"
#include "PWM.h"
#include "../Common/Common.h"
#include "StateMachine.h"
#include "../Common/debug.h"
#include "Probe_Drop.h"
#include "AttitudeManager.h"

int main(char input) {
    readDatalink(void);
    if (last_command_sent0 == (1 || 2 || 3)) {
        triggerCamera(input);
        dropProbe(1);
    }
    return 0;
}
